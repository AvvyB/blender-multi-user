# Multi-User Blender Server Configuration
# Copy this file to .env and customize your settings

# Admin password (required for admin privileges)
ADMIN_PASSWORD=your_secure_admin_password_here

# Server password (optional - leave empty for public server)
# If set, clients must provide this password to connect
SERVER_PASSWORD=

# Port to listen on (default: 5555)
# Note: This will also use PORT+1 and PORT+2 for data and heartbeat
PORT=5555

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Connection timeout in milliseconds (default: 10000 = 10 seconds)
TIMEOUT=10000

# === Persistent Storage Settings ===

# Auto-save interval in seconds (default: 120 = 2 minutes)
# Session data is automatically saved at this interval
SAVE_INTERVAL=120

# Maximum number of backup snapshots to keep (default: 10)
# Older backups are automatically deleted
MAX_BACKUPS=10
